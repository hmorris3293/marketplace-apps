---
- name: Set up MOTD
  template:
    src: templates/motd.j2
    dest: /etc/motd

- name: Writing creds into file
  copy:
    dest: '/home/{{ username }}/.credentials'
    mode: '0600'
    owner: '{{ username }}'
    group: '{{ username }}'
    content: |
      Sudo Username: {{ username }}
      Sudo Password: {{ password }}
      Apache Airflow User: airflow
      Apache Airflow Password: {{ airflow_password }}
      Postgres User: airflow
      Postgres Password: {{ postgres_password }}
