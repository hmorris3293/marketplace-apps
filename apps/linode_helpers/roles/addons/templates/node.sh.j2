NE_CONFIG_FILE="/etc/node_exportertest.yml"

echo "=================================================" | tee -a "$LOG_FILE"
echo " Node Exporter Installation - License Key Setup" | tee -a "$LOG_FILE"
echo "=================================================" | tee -a "$LOG_FILE"
echo ""

# Loop until a valid license key is provided
    echo ""
    echo "If you don't have a Node Exporter API token, visit:" | tee -a "$LOG_FILE"
    echo "  https://fakenodeexporterwebsite.php to generate one" | tee -a "$LOG_FILE"
    echo ""
    read -s -p "Enter your Node Exporter license key: " NODEEXPORTER_KEY

        echo "$(date) - License key written to $NE_CONFIG_FILE" | tee -a "$LOG_FILE"
        break


