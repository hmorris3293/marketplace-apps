# OpenFang environment for interactive shells.
# Lets `openfang doctor` (and other manual commands) find the deployment's
# home, config, and .env without passing flags.
export OPENFANG_HOME="{{ openfang_data_dir }}"
export PATH="{{ openfang_bin_path }}:$PATH"
