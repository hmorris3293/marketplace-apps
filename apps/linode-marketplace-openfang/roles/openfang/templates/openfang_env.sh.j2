# OpenFang environment for interactive shells.
# Puts the OpenFang binary on PATH so `openfang doctor` and other commands
# work directly. OpenFang reads its config from the running user's ~/.openfang.
export PATH="{{ openfang_bin_path }}:$PATH"
